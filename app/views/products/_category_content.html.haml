%section.items-box-container.clearfix
  %h3.text-center
    レディース 新着アイテム
  = render "product"
  = render "product"
  = render "product"
  = render "product"
  .view-all
    %a 全ての商品を見る

%section.items-box-container.clearfix
  %h3.text-center
    メンズ 新着アイテム
  = render "product"
  = render "product"
  = render "product"
  = render "product"
  .view-all
    %a 全ての商品を見る

%section.items-box-container.clearfix
  %h3.text-center
    ベビー・キッズ 新着アイテム
  = render "product"
  = render "product"
  = render "product"
  = render "product"
  .view-all
    %a 全ての商品を見る

%section.items-box-container.clearfix
  %h3.text-center
    コスメ・香水・美容 新着アイテム
  = render "product"
  = render "product"
  = render "product"
  = render "product"
  .view-all
    %a 全ての商品を見る

