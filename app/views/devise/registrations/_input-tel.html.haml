.signup-container
  %header.signup-header
    %h1
      %a{ href: "" }
        = image_tag("logo_signup_top", alt: :"mercari" ,class: "signup-header__logo")
      .signup-header__progress
        .progress
          .progress__item
            .progress__title.progress--done 会員情報
            .progress__dot.progress--done
              .fas.fa-circle
              .progress__line.progress--done
          .progress__item
            .progress__title.progress--done 電話番号確証
            .progress__dot.progress--done
              .fas.fa-circle
              .progress__line
          .progress__item
            .progress__title 住所入力
            .progress__dot
              .fas.fa-circle
              .progress__line
          .progress__item
            .progress__title 支払い方法
            .progress__dot
              .fas.fa-circle
              .progress__line
          .progress__item
            .progress__title 完了
            .progress__dot
              .fas.fa-circle
  %main.signup-main
    %section.l-signup-container
      .signup-main
        .signup-main__title 電話番号確証
        .signup-main__wrap
          = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
            = devise_error_messages!
            .field
              .input-form__name 携帯電話の番号
              %br/
              = f.text_field :tell, placeholder: '携帯電話の番号を入力', class: "signup_input"
              .identification__note 本人確認のため、携帯電話のSMS（ショートメッセージサービス）を利用して認証を行っています。
            .actions
              = f.submit "次へ進む", class: "signup_button"
            .identification__note ※電話番号は本人確認や不正利用を防ぐために利用します。他のユーザーに公開されることはございません。
  %footer.signup-footer
    = render "signup-footer"
